import React, { ReactNode } from "react";
import { View } from "react-native";

const HomeScreen: React.FC<{children: ReactNode}> = () => {
    return (
        <View></View>
    );
}

export default HomeScreen;